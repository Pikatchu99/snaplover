{
  "name": "e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Tests de régression bout en bout (Playwright) : web + signaling ensemble, 2+ navigateurs avec caméra factice.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "stickers:catalog": "node scripts/stickers/build-production-catalog.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
